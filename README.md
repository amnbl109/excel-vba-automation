# excel-vba-automation
Automated Excel Pivot Table generation system using VBA + Claude for Excel. Converts raw datasets into Data Model-driven pivot reports with dynamic rows, columns, values, aggregations, and multi-filter support through a configurable Pivot_Structure workflow.
